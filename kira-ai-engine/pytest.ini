[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers
markers =
    unit: fast pure-function tests with no I/O
    integration: TestClient-based endpoint tests (mocked dependencies only)
